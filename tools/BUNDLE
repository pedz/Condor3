# This is the command that cap productiom deploy executes at some
# point to create the bundled gems.
bundle install --path /home/condor/app-base/shared/bundle --without development test --deployment --quiet
